import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 7,
    },
    description: {
        flex: 1,
        fontSize: 14,
        fontWeight: 600,
        marginLeft: 16,
    },
});